<Window x:Class="UIAutomationInspectorWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UIAutomationInspectorWpf"
        mc:Ignorable="d"
        Title="UI Automation Inspector"
        Width="1180"
        Height="760"
        MinWidth="900"
        MinHeight="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Чеки через UI Automation" FontSize="24" FontWeight="SemiBold" VerticalAlignment="Center" />
            <TextBlock x:Name="StatusText" DockPanel.Dock="Right" Foreground="#64748B" VerticalAlignment="Center" Text="Готово" />
        </DockPanel>
        <TabControl Grid.Row="1">
            <TabItem Header="Обработка">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="Окно назначения:" VerticalAlignment="Center" Margin="0,0,8,0" />
                        <ComboBox x:Name="TargetWindowComboBox" Width="520" DisplayMemberPath="Title" />
                        <Button Content="Обновить окна" Click="RefreshWindows_Click" Margin="8,0,0,0" Padding="14,5" />
                    </StackPanel>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                        <Button Content="Выбрать Excel" Click="SelectExcel_Click" Padding="14,5" />
                        <TextBlock x:Name="ExcelPathText" Text="Файл не выбран" VerticalAlignment="Center" Margin="12,0,0,0" />
                        <Button x:Name="RunButton" Content="Запустить обработку" Click="RunButton_Click" Padding="14,5" Margin="24,0,0,0" IsEnabled="False" />
                        <Button x:Name="CancelButton" Content="Остановить" Click="CancelButton_Click" Padding="14,5" Margin="8,0,0,0" IsEnabled="False" />
                    </StackPanel>
                    <TextBox x:Name="PreviewTextBox" Grid.Row="2" IsReadOnly="True" AcceptsReturn="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" FontFamily="Consolas" Text="После выбора Excel здесь появится количество строк и групп ФД." />
                </Grid>
            </TabItem>
            <TabItem Header="Настройки">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <DockPanel Margin="0,0,0,12">
                        <StackPanel Orientation="Horizontal">
                            <Button Content="Сохранить настройки" Click="SaveProfile_Click" Padding="14,5" />
                            <Button Content="Сбросить профиль" Click="ResetProfile_Click" Padding="14,5" Margin="8,0,0,0" />
                        </StackPanel>
                        <TextBlock DockPanel.Dock="Right" Text="Нажмите «Выбрать», затем кликните нужный элемент в окне назначения" Foreground="#64748B" VerticalAlignment="Center" />
                    </DockPanel>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="ElementSettingsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#CBD5E1" BorderThickness="0,0,0,1" Padding="0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="220" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center" FontWeight="SemiBold" />
                                            <TextBlock Grid.Column="1" Text="{Binding}" Foreground="#64748B" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                            <Button Grid.Column="2" Content="Выбрать" Tag="{Binding Key}" Click="PickElement_Click" Padding="12,4" />
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </TabItem>
        </TabControl>
        <TextBlock Grid.Row="2" Text="Для корректной работы целевое приложение должно быть открыто и доступно Windows UI Automation. Excel-файл должен быть .xlsx." Foreground="#64748B" Margin="0,12,0,0" />
    </Grid>
</Window>
